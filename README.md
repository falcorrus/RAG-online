# 🧠 AI RAG Widget (Knowledge Base)

Легковесный и красивый виджет поиска по базе знаний с поддержкой AI. Предназначен для быстрой интеграции на любые веб-сайты (HTML/WP/React).

## 🚀 Основные возможности
- **Минималистичный дизайн**: Темная тема, glassmorphism, плавные анимации.
- **Умный поиск**: Имитация RAG (Retrieval Augmented Generation) с "эффектом печатания".
- **Адаптивность**: Работает на десктопах и мобильных.
- **Два режима**:
  - **Fullview**: Полноэкранный поиск по центру.
  - **Mini Widget**: Сворачивается в плавающую кнопку (✨) в углу экрана.

## 🛠 Технологии
- **Frontend**: Чистый HTML5, CSS3 (CSS Variables), Vanilla JS (ES6+). Без фреймворков и зависимостей.
- **Backend (в планах)**: n8n + Google Docs/Sheets в качестве источника знаний.
- **Hosting**: Статика через Nginx на VPS (`rag.reloto.ru`).

## 📂 Структура проекта
```bash
.
├── index.html      # Разметка виджета
├── style.css       # Стили (анимации, тема, адаптив)
├── script.js       # Логика (поиск, сворачивание, моки ответов)
├── deploy.sh       # Скрипт мгновенного деплоя
└── README.md       # Эта документация
```

## ⚡️ Как развернуть и обновить

### Локальная разработка
Просто откройте `index.html` в браузере. Сервер не требуется.

### Деплой на продакшн (VPS)
Для обновления сайта используйте скрипт `deploy.sh`. Он автоматически коммитит изменения и отправляет их на сервер и GitHub.

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x deploy.sh

# Обновить сайт
./deploy.sh "Добавил новую фичу"
```

## 🔧 Конфигурация Nginx (VPS)
Файлы автоматически попадают в `/var/www/rag.reloto.ru` через Git Hook.
Конфигурация сервера: `/etc/nginx/sites-enabled/rag.reloto.ru`.

## 🔮 План интеграции (Roadmap)
1.  **Подключение n8n**: Заменить `mockKnowledgeBase` в `script.js` на реальный вызов Webhook n8n.
2.  **Источник данных**: Настроить Google Docs как источник знаний для n8n.
3.  **Embed**: Упаковать JS/CSS в один файл для вставки на сторонние сайты одной строкой кода.
