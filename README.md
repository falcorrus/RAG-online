# RAG Online 🤖

Легкая и красивая веб-база знаний с умным поиском на основе **Google Gemini 2.0 Flash**.
Проект работает как Single Page Application (SPA) с минималистичным бекендом для безопасного проксирования запросов к ИИ.

## ✨ Возможности

*   **Умный поиск (RAG):** ИИ анализирует вашу базу знаний и отвечает на вопросы естественным языком.
*   **Загрузка данных:** Просто перетащите `.md` файл с вашей документацией в панель настроек.
*   **Гибридный режим:** Если ИИ недоступен, автоматически включается мощный локальный поиск по ключевым словам и корням.
*   **Glassmorphism UI:** Современный, адаптивный интерфейс с анимациями.
*   **Безопасность:** API-ключ хранится только на сервере в `.env` файле и не виден пользователям.

## 🚀 Архитектура

*   **Frontend:** HTML5, CSS3, Vanilla JS (без фреймворков).
*   **Backend:** Python (FastAPI) — работает как прокси для скрытия API-ключа.
*   **AI:** Google Gemini 2.0 Flash.

## 🛠 Установка и Запуск

### 1. Локальная разработка (Frontend + Backend)

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/falcorrus/RAG-online.git
    cd RAG-online
    ```

2.  Создайте файл `.env` в корне проекта и добавьте ваш ключ:
    ```env
    GEMINI_API_KEY=ваш_ключ_от_google_ai_studio
    ```

3.  Установите зависимости и запустите сервер:
    ```bash
    pip install -r requirements.txt
    uvicorn server:app --reload
    ```

4.  Откройте `index.html` в браузере (через Live Server или `python -m http.server`).

### 2. Деплой

Деплой выполняется максимально просто — через отправку изменений в репозиторий:
1. Запустите скрипт деплоя: `./deploy.sh "ваше сообщение"`
2. Скрипт зафиксирует изменения и отправит их на VPS и GitHub.
3. VPS настроен на автоматическое обновление файлов и перезапуск сервера при получении новых данных в ветку `master`.

## 📝 Как пользоваться

1.  Откройте сайт.
2.  Нажмите на иконку ⚙️ (настройки) в правом верхнем углу.
3.  Перетащите ваш `.md` файл с базой знаний в зону загрузки.
4.  (Опционально) Выберите язык по умолчанию.
5.  Начните задавать вопросы в чате!

## 🛡 Безопасность

*   API-ключ никогда не передается на клиент.
*   Бекенд принимает запросы только на эндпоинт `/api/chat`.
*   Файл `.env` исключен из Git.

---

Что сделано:
   1. Backend (надежность и изоляция):
       * Данные теперь хранятся в opt/RAG-online/storage/ в виде JSON и отдельных .md файлов.
       * Внедрена JWT-авторизация (токены) и хэширование паролей (bcrypt).
       * Ваша почта ekirshin@gmail.com автоматически получает статус админа при регистрации.
       * Удалена старая проверка Nginx Basic Auth — теперь все управляется через интерфейс приложения.


   2. Frontend (новый UX):
       * При нажатии на ⚙️ открывается окно Входа/Регистрации.
       * После входа сессия сохраняется (токен лежит в localStorage), и настройки становятся доступны.
       * База знаний теперь загружается на сервер и привязана к вашему аккаунту. При поиске ИИ использует именно ваш
         файл.


   3. Безопасность:
       * Пароли в базе хранятся в виде необратимых хэшей.
       * Поиск и настройки доступны только после авторизации.


  Как проверить:
   1. Зайдите на https://rag.reloto.ru/.
   2. Нажмите на шестеренку ⚙️.
   3. Выберите "Регистрация" и создайте аккаунт.
   4. Загрузите свой .md файл в настройках.
   5. Теперь вы можете искать информацию именно по своему файлу.

---





## 📄 Лицензия

MIT